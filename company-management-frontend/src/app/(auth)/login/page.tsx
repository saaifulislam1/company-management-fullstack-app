// All form logic is encapsulated in the LoginForm component for better organization.
import { LoginForm } from "@/components/shared/LoginForm";

export default function LoginPage() {
  return <LoginForm />;
}
